# Session configuration
SESSION_SECRET=dev-secret-key-change-in-production-environment

# Database configuration
DATABASE_URL=sqlite:///timetable.db

# Supabase configuration (optional - will use basic auth if not configured)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# OpenAI configuration (optional - AI features will be disabled if not configured)
OPENAI_API_KEY=